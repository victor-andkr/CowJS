import { cow } from "../../../../constants.js";
                function updatePlayerValue(index, value, updateView) {
                    if (!cow.player) return
                    cow.player[index] = value
                }
                
export default updatePlayerValue;
/***/
